// scripts.js

/*let points = 0;

function earnPoints() {
    points += 10;
    document.getElementById('point-count').innerText = points;
    updateBadge();
}

function updateBadge() {
    let badgeDisplay = document.getElementById('badge-display');
    if (points >= 50) {
        badgeDisplay.innerText = "Fan de Lang Lang";
        badgeDisplay.style.color = "gold";
    } else {
        badgeDisplay.innerText = "Ninguna";
        badgeDisplay.style.color = "black";
    }
}*/
